Name: Justine Le 
1. I would fit the the automates tests within a Github action that runs whenever code is pushed, since not only is this the most reliable option, but also catches any bugs before the code can cause conflicts and infiltrate the remote repository. 
2. no
3. Navigation mode takes a screenshot right after the page loads, while snapshot mode takes a screenshot on demand at any point during a test. 
4. Based on the Lighthouse results, it recommends to avoid chaining critical requests, use efficient cache lifetimes, and render-blocking requests.  






